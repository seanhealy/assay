export interface LiquidPresetOptions {
	/** Paths to directories containing .liquid template files */
	liquidPaths?: string[];
	/** Path to the theme assets directory. Defaults to './assets' */
	assetsPath?: string;
}
